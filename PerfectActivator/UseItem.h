#include "LPPConstants.h"
#include "Biscuit.h"

class UseItems
{
public:
	void UseItem(bool combo = false)
	{
		if (!GEntityList->Player()->HasBuffOfType(BUFF_Invulnerability))
		{
			
		}
	}
};
